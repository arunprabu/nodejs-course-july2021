// npm i chai

// const { assert, expect } = require('chai');
// const should = require('chai').should();

// const scores = [10, 200, 450, 120, 20];

// assert.isArray(scores, 'Scores -- Not an Array');
// assert.include(scores, 200, 'Array doesn\'t have 340');
// assert.lengthOf(scores, 5, 'Array length is not 5');

// expect(scores).to.be.an('array').that.includes(450);
// expect(scores).to.have.lengthOf(5);


// let x = '10';
// x.should.be.a('string').that.includes('10');
// scores.should.have.lengthOf(6);